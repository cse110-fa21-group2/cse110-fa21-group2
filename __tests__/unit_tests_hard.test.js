//  We can split the files and the script should
//  automatically run all tests

/**
 * @param name
 * @param fn
 * @param timeout
 */
// eslint-disable-next-line no-undef
test('adds 1 + 1 + 1 to equal 3', () => {
  // eslint-disable-next-line no-undef
  expect(1 + 1 + 1).toBe(3);
});

// eslint-disable-next-line no-undef
test('adds some numbers to equal 3', () => {
  // eslint-disable-next-line no-undef
  expect(1 + 5 + 1).toBe(7);
});
